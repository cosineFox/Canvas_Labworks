# Anubis - AI Web Firewall / Bot Protection
# https://github.com/TecharoHQ/anubis
FROM ghcr.io/techarohq/anubis:latest

# Copy bot policy configuration
COPY anubis-policy.json /etc/anubis/policy.json

# Set policy file path
ENV POLICY_FNAME=/etc/anubis/policy.json

# Render uses PORT env var dynamically
EXPOSE 10000
